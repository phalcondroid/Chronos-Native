
class Operators
{
    static OR          = "$or";
    static AND         = "$and";
    static SORT        = "$sort";
    static IS_NOT      = "$isNot";
    static LIMIT       = "$limit";
    static COLUMNS     = "$columns";
    static CONDITIONAL = "$conditions";
}